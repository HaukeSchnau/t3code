name: Project Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    uses: HaukeSchnau/nix-infra-modules/.github/workflows/project-release.yml@main
    with:
      verification_check: projectReleaseGate
