name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: t3code-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NIX_CONFIG: experimental-features = nix-command flakes
  CI_PROJECT_ID: t3code

jobs:
  check:
    name: Format, lint, and type-check
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@v4

      - name: Run checks
        run: |
          set -euo pipefail
          if [ -x /run/current-system/sw/bin/nix ]; then
            export PATH="/run/current-system/sw/bin:$PATH"
          fi
          source_workspace="$GITHUB_WORKSPACE"
          nix develop .#ci --command \
            "$source_workspace/scripts/ci-workspace-run.sh" \
            "$source_workspace" just qa-check

  test_non_server:
    name: Test packages and clients
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@v4

      - name: Run tests
        run: |
          set -euo pipefail
          if [ -x /run/current-system/sw/bin/nix ]; then
            export PATH="/run/current-system/sw/bin:$PATH"
          fi
          source_workspace="$GITHUB_WORKSPACE"
          nix develop .#ci --command \
            "$source_workspace/scripts/ci-workspace-run.sh" \
            "$source_workspace" just qa-test-non-server

  test_server:
    name: Test server ${{ matrix.shard }}/3
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@v4

      - name: Run server test shard
        run: |
          set -euo pipefail
          if [ -x /run/current-system/sw/bin/nix ]; then
            export PATH="/run/current-system/sw/bin:$PATH"
          fi
          source_workspace="$GITHUB_WORKSPACE"
          nix develop .#ci --command \
            "$source_workspace/scripts/ci-workspace-run.sh" \
            "$source_workspace" just qa-test-server-shard '${{ matrix.shard }}' 3

  release_smoke:
    name: Release smoke
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@v4

      - name: Exercise release-only steps
        run: |
          set -euo pipefail
          if [ -x /run/current-system/sw/bin/nix ]; then
            export PATH="/run/current-system/sw/bin:$PATH"
          fi
          source_workspace="$GITHUB_WORKSPACE"
          nix develop .#ci --command \
            "$source_workspace/scripts/ci-workspace-run.sh" \
            "$source_workspace" just qa-release

  release_check:
    name: Release contract
    needs: [check, test_non_server, test_server, release_smoke]
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@v4

      - name: Verify release contract
        run: |
          set -euo pipefail
          if [ -x /run/current-system/sw/bin/nix ]; then
            export PATH="/run/current-system/sw/bin:$PATH"
          fi
          system="$(nix eval --impure --raw --expr builtins.currentSystem)"
          nix build ".#checks.${system}.projectReleaseGate" --no-link --print-build-logs

  release:
    name: Release
    needs: release_check
    uses: schnau/nix-infra-modules/.gitea/workflows/project-release.yml@main
